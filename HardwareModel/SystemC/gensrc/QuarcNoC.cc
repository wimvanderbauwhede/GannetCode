// Generated with ../build/genNoC.pl

 (*instances[ 0 ]).network_rx_fifo0 .bind ((*instances[ 1 ]).xpwr_rxfifo0);
 (*instances[ 0 ]).network_rx_fifo1 .bind ((*instances[ 8 ]).xpwr_rxfifo1);
 (*instances[ 0 ]).network_rx_fifo2 .bind ((*instances[ 8 ]).xpwr_rxfifo2);
 (*instances[ 0 ]).network_rx_fifo3 .bind (gwinstance.xpwr_rxfifo3);

 (*instances[ 1 ]).network_rx_fifo0 .bind ((*instances[ 2 ]).xpwr_rxfifo0);
 (*instances[ 1 ]).network_rx_fifo1 .bind ((*instances[ 9 ]).xpwr_rxfifo1);
 (*instances[ 1 ]).network_rx_fifo2 .bind ((*instances[ 9 ]).xpwr_rxfifo2);
 (*instances[ 1 ]).network_rx_fifo3 .bind ((*instances[ 0 ]).xpwr_rxfifo3);

 (*instances[ 2 ]).network_rx_fifo0 .bind ((*instances[ 3 ]).xpwr_rxfifo0);
 (*instances[ 2 ]).network_rx_fifo1 .bind ((*instances[ 10 ]).xpwr_rxfifo1);
 (*instances[ 2 ]).network_rx_fifo2 .bind ((*instances[ 10 ]).xpwr_rxfifo2);
 (*instances[ 2 ]).network_rx_fifo3 .bind ((*instances[ 1 ]).xpwr_rxfifo3);

 (*instances[ 3 ]).network_rx_fifo0 .bind ((*instances[ 4 ]).xpwr_rxfifo0);
 (*instances[ 3 ]).network_rx_fifo1 .bind ((*instances[ 11 ]).xpwr_rxfifo1);
 (*instances[ 3 ]).network_rx_fifo2 .bind ((*instances[ 11 ]).xpwr_rxfifo2);
 (*instances[ 3 ]).network_rx_fifo3 .bind ((*instances[ 2 ]).xpwr_rxfifo3);

 (*instances[ 4 ]).network_rx_fifo0 .bind ((*instances[ 5 ]).xpwr_rxfifo0);
 (*instances[ 4 ]).network_rx_fifo1 .bind ((*instances[ 12 ]).xpwr_rxfifo1);
 (*instances[ 4 ]).network_rx_fifo2 .bind ((*instances[ 12 ]).xpwr_rxfifo2);
 (*instances[ 4 ]).network_rx_fifo3 .bind ((*instances[ 3 ]).xpwr_rxfifo3);

 (*instances[ 5 ]).network_rx_fifo0 .bind ((*instances[ 6 ]).xpwr_rxfifo0);
 (*instances[ 5 ]).network_rx_fifo1 .bind ((*instances[ 13 ]).xpwr_rxfifo1);
 (*instances[ 5 ]).network_rx_fifo2 .bind ((*instances[ 13 ]).xpwr_rxfifo2);
 (*instances[ 5 ]).network_rx_fifo3 .bind ((*instances[ 4 ]).xpwr_rxfifo3);

 (*instances[ 6 ]).network_rx_fifo0 .bind ((*instances[ 7 ]).xpwr_rxfifo0);
 (*instances[ 6 ]).network_rx_fifo1 .bind ((*instances[ 14 ]).xpwr_rxfifo1);
 (*instances[ 6 ]).network_rx_fifo2 .bind ((*instances[ 14 ]).xpwr_rxfifo2);
 (*instances[ 6 ]).network_rx_fifo3 .bind ((*instances[ 5 ]).xpwr_rxfifo3);

 (*instances[ 7 ]).network_rx_fifo0 .bind ((*instances[ 8 ]).xpwr_rxfifo0);
 (*instances[ 7 ]).network_rx_fifo1 .bind (gwinstance.xpwr_rxfifo1);
 (*instances[ 7 ]).network_rx_fifo2 .bind (gwinstance.xpwr_rxfifo2);
 (*instances[ 7 ]).network_rx_fifo3 .bind ((*instances[ 6 ]).xpwr_rxfifo3);

 (*instances[ 8 ]).network_rx_fifo0 .bind ((*instances[ 9 ]).xpwr_rxfifo0);
 (*instances[ 8 ]).network_rx_fifo1 .bind ((*instances[ 0 ]).xpwr_rxfifo1);
 (*instances[ 8 ]).network_rx_fifo2 .bind ((*instances[ 0 ]).xpwr_rxfifo2);
 (*instances[ 8 ]).network_rx_fifo3 .bind ((*instances[ 7 ]).xpwr_rxfifo3);

 (*instances[ 9 ]).network_rx_fifo0 .bind ((*instances[ 10 ]).xpwr_rxfifo0);
 (*instances[ 9 ]).network_rx_fifo1 .bind ((*instances[ 1 ]).xpwr_rxfifo1);
 (*instances[ 9 ]).network_rx_fifo2 .bind ((*instances[ 1 ]).xpwr_rxfifo2);
 (*instances[ 9 ]).network_rx_fifo3 .bind ((*instances[ 8 ]).xpwr_rxfifo3);

 (*instances[ 10 ]).network_rx_fifo0 .bind ((*instances[ 11 ]).xpwr_rxfifo0);
 (*instances[ 10 ]).network_rx_fifo1 .bind ((*instances[ 2 ]).xpwr_rxfifo1);
 (*instances[ 10 ]).network_rx_fifo2 .bind ((*instances[ 2 ]).xpwr_rxfifo2);
 (*instances[ 10 ]).network_rx_fifo3 .bind ((*instances[ 9 ]).xpwr_rxfifo3);

 (*instances[ 11 ]).network_rx_fifo0 .bind ((*instances[ 12 ]).xpwr_rxfifo0);
 (*instances[ 11 ]).network_rx_fifo1 .bind ((*instances[ 3 ]).xpwr_rxfifo1);
 (*instances[ 11 ]).network_rx_fifo2 .bind ((*instances[ 3 ]).xpwr_rxfifo2);
 (*instances[ 11 ]).network_rx_fifo3 .bind ((*instances[ 10 ]).xpwr_rxfifo3);

 (*instances[ 12 ]).network_rx_fifo0 .bind ((*instances[ 13 ]).xpwr_rxfifo0);
 (*instances[ 12 ]).network_rx_fifo1 .bind ((*instances[ 4 ]).xpwr_rxfifo1);
 (*instances[ 12 ]).network_rx_fifo2 .bind ((*instances[ 4 ]).xpwr_rxfifo2);
 (*instances[ 12 ]).network_rx_fifo3 .bind ((*instances[ 11 ]).xpwr_rxfifo3);

 (*instances[ 13 ]).network_rx_fifo0 .bind ((*instances[ 14 ]).xpwr_rxfifo0);
 (*instances[ 13 ]).network_rx_fifo1 .bind ((*instances[ 5 ]).xpwr_rxfifo1);
 (*instances[ 13 ]).network_rx_fifo2 .bind ((*instances[ 5 ]).xpwr_rxfifo2);
 (*instances[ 13 ]).network_rx_fifo3 .bind ((*instances[ 12 ]).xpwr_rxfifo3);

 (*instances[ 14 ]).network_rx_fifo0 .bind (gwinstance.xpwr_rxfifo0);
 (*instances[ 14 ]).network_rx_fifo1 .bind ((*instances[ 6 ]).xpwr_rxfifo1);
 (*instances[ 14 ]).network_rx_fifo2 .bind ((*instances[ 6 ]).xpwr_rxfifo2);
 (*instances[ 14 ]).network_rx_fifo3 .bind ((*instances[ 13 ]).xpwr_rxfifo3);

 gwinstance.network_rx_fifo0 .bind ((*instances[ 0 ]).xpwr_rxfifo0);
 gwinstance.network_rx_fifo1 .bind ((*instances[ 7 ]).xpwr_rxfifo1);
 gwinstance.network_rx_fifo2 .bind ((*instances[ 7 ]).xpwr_rxfifo2);
 gwinstance.network_rx_fifo3 .bind ((*instances[ 14 ]).xpwr_rxfifo3);
